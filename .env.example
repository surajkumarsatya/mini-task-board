# This .env.example file only shows the required environment variable format.
# It does not contain any real credentials or database passwords.
# Create a .env file using this structure and add your local credentials. 

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=mini_task_board